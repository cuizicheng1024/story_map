# Debug Session: map-loading-blank
- **Status**: [OPEN]
- **Issue**: 人物页地图已切到高德备用底图，但底图区域仍然近乎空白，只剩标记点与控件；同时需要给李白页的“延伸观看：李白：从长安到夜郎。”增加可感知的特殊字体样式。
- **Scope**: `storymap/script/profile/renderer.py`、人物页模板与 `李白` 页面内容
- **Hypotheses**:
  1. 高德备用底图初始化成功了，但没有正确设置中心点或缩放级别，导致瓦片实际加载范围偏离标记点。
  2. GeoVis 失败后的 fallback 状态没有把底图类型、图层或容器尺寸完整同步到高德实例，出现“有 marker、无底图”的半初始化状态。
  3. 高德瓦片请求本身失败了，但页面只展示了 GeoVis 失败提示，没有把高德底图加载错误暴露出来。
  4. 人物页地图容器在切换底图后尺寸计算异常，导致 tiles 没有触发正确重排，只看到覆盖物和 UI。
  5. 李白页“延伸观看”文案目前是普通正文渲染，缺少专门样式钩子或模板识别逻辑，所以用户感知不明显。

## Plan
1. 启动调试日志采集服务，给人物页地图初始化路径加最小插桩。
2. 复现 `李白` 页面地图加载过程，收集 GeoVis 失败后 fallback 到高德的运行时证据。
3. 基于日志判定根因后做最小修复。
4. 修复后复测地图与李白页“延伸观看”样式。
