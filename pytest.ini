[pytest]
testpaths =
    tools/build
    tests
python_files =
    test_*.py
    *_test.py
addopts =
    --strict-markers
    --tb=short
    -v
filterwarnings =
    ignore::DeprecationWarning:tools.build.sync_knowledge_audit
    ignore::DeprecationWarning:tools.build.fix_coordinates
