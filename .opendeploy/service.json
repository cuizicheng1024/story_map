{
  "name": "mapsotryforstudents",
  "type": "web",
  "port": 8765,
  "health_check_path": "/health",
  "build_command": "pip install -r requirements.txt",
  "start_command": "sh -c \"PYTHONPATH=/app:${PYTHONPATH:-} python3 -m storymap.script.cli.story_map --serve --port ${PORT:-8765}\"",
  "env": {
    "PYTHONPATH": "/app"
  }
}
